### Chat App
I wrote this app in Node.js lesson <br />
#### Run the application
`npm install` and `npm run dev` <br /> 
To see the output on a web based interface, open the link `http://127.0.0.1/3000`
